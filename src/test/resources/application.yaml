spring:
  application.name: automobile-betting
  jackson:
    deserialization:
      FAIL-ON-UNKNOWN-PROPERTIES: false
    property-naming-strategy: SNAKE_CASE
    time-zone: UTC
  cloud:
    openfeign.client.config:
      openf1-api:
        connect-timeout: 500
        read-timeout: 1000
        url: http://localhost:${wiremock.server.port}